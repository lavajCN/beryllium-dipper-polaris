name: Build eXtremeKernel

on:
  push:
    tags:
      - 'v9.3*'
  workflow_dispatch:
    inputs:
      device:
        description: 'Select device to build for'
        required: true
        default: 'dipper'
        type: choice
        options:
          - dipper
          - equuleus
          - ursa
          - beryllium
          - perseus
          - polaris

jobs:
  build_kernel:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        submodules: 'recursive'

    - name: Set up environment
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential libncurses-dev libssl-dev \
          flex bison libelf-dev bc git gcc-aarch64-linux-gnu ccache xz-utils

    - name: Ensure WireGuard exists
  run: |
    mkdir -p net/wireguard/crypto
    # 下载 Kconfig 并验证
    if ! curl -L -s -o net/wireguard/Kconfig.tmp https://git.zx2c4.com/wireguard-linux-compat/plain/Kconfig || ! grep -q 'config WIREGUARD' net/wireguard/Kconfig.tmp; then
      echo "下载 WireGuard Kconfig 失败，使用备用内容"
      cat > net/wireguard/Kconfig <<EOF
config WIREGUARD
        tristate "WireGuard secure network tunnel"
        default m
        help
          WireGuard is a secure, fast, and easy to use replacement for IPsec.

          See <https://www.wireguard.com/> for more information.

          If unsure, say Y.

config WIREGUARD_DEBUG
        bool "Debugging checks and verbose messages"
        depends on WIREGUARD
        help
          This will write verbose messages to the system log, and enable
          extra sanity checks, for example, about the validity of certain
          packet structures.

          Only say Y here if you are debugging a problem.
EOF
    else
      mv net/wireguard/Kconfig.tmp net/wireguard/Kconfig
    fi

    # 下载 Makefile
    if ! curl -L -s -o net/wireguard/Makefile https://git.zx2c4.com/wireguard-linux-compat/plain/Makefile || ! grep -q 'obj-\$(CONFIG_WIREGUARD)' net/wireguard/Makefile; then
      echo "下载 WireGuard Makefile 失败，使用备用内容"
      echo 'obj-$(CONFIG_WIREGUARD) += wireguard.o' > net/wireguard/Makefile
      echo 'wireguard-y := allowedips.o cookie.o netlink.o peer.o peerlookup.o queueing.o ratelimiter.o routingtable.o noise.o' >> net/wireguard/Makefile
      echo 'wireguard-y += device.o timers.o' >> net/wireguard/Makefile
    fi

    # 下载 zinc.h
    mkdir -p net/wireguard/crypto
    curl -L -s -o net/wireguard/crypto/zinc.h https://git.zx2c4.com/wireguard-linux-compat/plain/crypto/zinc.h || touch net/wireguard/crypto/zinc.h

    # 修改 Kconfig 类型
    sed -i 's/tristate/bool/;s/default m/default y/;' net/wireguard/Kconfig
    touch net/wireguard/.check

    - name: Show WireGuard files
  run: |
    echo "--- Kconfig ---"
    cat net/wireguard/Kconfig
    echo "--- Makefile ---"
    cat net/wireguard/Makefile
    echo "--- zinc.h ---"
    head -n 10 net/wireguard/crypto/zinc.h
    
    - name: Set device config
      id: set_config
      run: |
        case "${{ github.event.inputs.device || 'beryllium' }}" in
          dipper)
            echo "defconfig=mi8_defconfig" >> $GITHUB_ENV
            echo "DEVICE_NAME=Xiaomi Mi 8" >> $GITHUB_ENV
            ;;
          equuleus)
            echo "defconfig=mi8pro_defconfig" >> $GITHUB_ENV
            echo "DEVICE_NAME=Xiaomi Mi 8 Pro" >> $GITHUB_ENV
            ;;
          ursa)
            echo "defconfig=ursa_defconfig" >> $GITHUB_ENV
            echo "DEVICE_NAME=Xiaomi Mi 8 Explorer Edition" >> $GITHUB_ENV
            ;;
          beryllium)
            echo "defconfig=poco_defconfig" >> $GITHUB_ENV
            echo "DEVICE_NAME=POCO F1" >> $GITHUB_ENV
            ;;
          perseus)
            echo "defconfig=perseus_defconfig" >> $GITHUB_ENV
            echo "DEVICE_NAME=Xiaomi Mi MIX 3" >> $GITHUB_ENV
            ;;
          polaris)
            echo "defconfig=mix2s_defconfig" >> $GITHUB_ENV
            echo "DEVICE_NAME=Xiaomi Mi MIX 2S" >> $GITHUB_ENV
            ;;
        esac
        echo "Building for $DEVICE_NAME with $defconfig"

    - name: Verify tag checkout
      run: |
        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
          git checkout v9.3 || git checkout tags/v9.3
        else
          git describe --tags --exact-match
        fi

    - name: Build configuration
      run: |
        make O=out ${{ env.defconfig }}

    - name: Compile kernel
      run: |
        make -j$(nproc) O=out \
        CC="ccache gcc" \
        CROSS_COMPILE=$CROSS_COMPILE \
        KBUILD_BUILD_USER=GitHub-Actions_lavaj \
        KBUILD_BUILD_HOST=Kernel-Builder \
        LOCALVERSION="-eXtreme-${{ env.DEVICE_NAME }}"

    - name: Package artifacts
      run: |
        mkdir artifacts
        cp out/arch/arm64/boot/Image.gz-dtb artifacts/
        find out/ -name "*.ko" -exec cp {} artifacts/ \;
        tar -czvf kernel-${{ env.DEVICE_NAME }}.tar.gz artifacts/
        echo "DEVICE_NAME=$DEVICE_NAME" >> $GITHUB_ENV

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: kernel-${{ env.DEVICE_NAME }}-build
        path: kernel-${{ env.DEVICE_NAME }}.tar.gz
